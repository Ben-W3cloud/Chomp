import 'package:flutter/material.dart';

class ScanLogView extends StatelessWidget {
  const ScanLogView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
