import 'package:flutter/material.dart';

class FeedItemTile extends StatelessWidget {
  const FeedItemTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
