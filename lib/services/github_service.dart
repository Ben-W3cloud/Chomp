import 'package:flutter/material.dart';

class GithubService extends StatelessWidget {
  const GithubService({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
