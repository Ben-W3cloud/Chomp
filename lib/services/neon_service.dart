import 'package:flutter/material.dart';

class NeonService extends StatelessWidget {
  const NeonService({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
