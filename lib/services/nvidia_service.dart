import 'package:flutter/material.dart';

class NvidiaService extends StatelessWidget {
  const NvidiaService({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
