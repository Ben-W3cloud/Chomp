import 'package:flutter/material.dart';

class FcmService extends StatelessWidget {
  const FcmService({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
