import 'package:flutter/material.dart';

class ScanEngine extends StatelessWidget {
  const ScanEngine({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
