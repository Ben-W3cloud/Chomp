import 'package:flutter/material.dart';

class RepoProvider extends StatelessWidget {
  const RepoProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
